@extends('layouts.app')
@section('title','Asistencias')
@section('header','Asistencias')
@section('content')
<div class="soft-card p-3"><div class="d-flex flex-wrap justify-content-between gap-3 mb-3"><form class="d-flex flex-wrap gap-2" method="GET"><select name="empleado_id" class="form-select"><option value="">Empleado</option>@foreach($empleados as $empleado)<option value="{{ $empleado->id }}" @selected(request('empleado_id')==$empleado->id)>{{ $empleado->nombre_completo }}</option>@endforeach</select><input type="date" name="fecha" value="{{ request('fecha') }}" class="form-control"><button class="btn btn-outline-primary"><i class="bi bi-filter"></i></button></form>@if(auth()->user()->role !== 'Empleado')<a class="btn btn-primary" href="{{ route('asistencias.create') }}"><i class="bi bi-plus-lg me-1"></i>Nueva asistencia</a>@endif</div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Empleado</th><th>Fecha</th><th>Entrada</th><th>Salida</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($asistencias as $asistencia)<tr><td>{{ $asistencia->empleado->nombre_completo }}</td><td>{{ $asistencia->fecha->format('d/m/Y') }}</td><td>{{ $asistencia->hora_entrada }}</td><td>{{ $asistencia->hora_salida }}</td><td>@include('partials.badge',['estado'=>$asistencia->estado])</td><td class="text-end"><a class="btn btn-sm btn-outline-info action-btn" href="{{ route('asistencias.show',$asistencia) }}"><i class="bi bi-eye"></i></a>@if(auth()->user()->role !== 'Empleado') <a class="btn btn-sm btn-outline-primary action-btn" href="{{ route('asistencias.edit',$asistencia) }}"><i class="bi bi-pencil"></i></a> <form class="d-inline" method="POST" action="{{ route('asistencias.destroy',$asistencia) }}" data-confirm="Eliminar asistencia?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger action-btn"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">Sin asistencias.</td></tr>@endforelse</tbody></table></div>{{ $asistencias->links() }}</div>
@endsection
