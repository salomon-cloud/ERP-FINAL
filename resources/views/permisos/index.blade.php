@extends('layouts.app')
@section('title','Permisos')
@section('header','Permisos y vacaciones')
@section('content')
<div class="soft-card p-3"><div class="d-flex flex-wrap justify-content-between gap-3 mb-3"><form class="d-flex flex-wrap gap-2" method="GET"><select name="empleado_id" class="form-select"><option value="">Empleado</option>@foreach($empleados as $empleado)<option value="{{ $empleado->id }}" @selected(request('empleado_id')==$empleado->id)>{{ $empleado->nombre_completo }}</option>@endforeach</select><select name="estado" class="form-select"><option value="">Estado</option>@foreach(['pendiente','aprobado','rechazado'] as $estado)<option value="{{ $estado }}" @selected(request('estado')===$estado)>{{ ucfirst($estado) }}</option>@endforeach</select><button class="btn btn-outline-primary"><i class="bi bi-filter"></i></button></form><a class="btn btn-primary" href="{{ route('permisos.create') }}"><i class="bi bi-plus-lg me-1"></i>Nueva solicitud</a></div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Empleado</th><th>Tipo</th><th>Periodo</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($permisos as $permiso)<tr><td>{{ $permiso->empleado->nombre_completo }}</td><td>{{ ucfirst($permiso->tipo) }}</td><td>{{ $permiso->fecha_inicio->format('d/m/Y') }} - {{ $permiso->fecha_fin->format('d/m/Y') }}</td><td>@include('partials.badge',['estado'=>$permiso->estado])</td><td class="text-end"><a class="btn btn-sm btn-outline-info action-btn" href="{{ route('permisos.show',$permiso) }}"><i class="bi bi-eye"></i></a>@if(auth()->user()->role !== 'Empleado') <a class="btn btn-sm btn-outline-primary action-btn" href="{{ route('permisos.edit',$permiso) }}"><i class="bi bi-pencil"></i></a> <form class="d-inline" method="POST" action="{{ route('permisos.aprobar',$permiso) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success action-btn"><i class="bi bi-check2"></i></button></form> <form class="d-inline" method="POST" action="{{ route('permisos.rechazar',$permiso) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-warning action-btn"><i class="bi bi-x-circle"></i></button></form> <form class="d-inline" method="POST" action="{{ route('permisos.destroy',$permiso) }}" data-confirm="Eliminar solicitud?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger action-btn"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">Sin solicitudes.</td></tr>@endforelse</tbody></table></div>{{ $permisos->links() }}</div>
@endsection
