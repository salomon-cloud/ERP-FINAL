@extends('layouts.app')
@section('header',$puesto->nombre)
@section('content')<div class="soft-card p-4"><div class="d-flex justify-content-between"><div><h2 class="h4 fw-bold">{{ $puesto->nombre }}</h2><p class="text-muted">{{ $puesto->departamento->nombre }}</p></div>@include('partials.badge',['estado'=>$puesto->estado])</div><p>{{ $puesto->descripcion }}</p><p><strong>Rango salarial:</strong> ${{ number_format($puesto->sueldo_minimo,2) }} - ${{ number_format($puesto->sueldo_maximo,2) }}</p><h3 class="h5 fw-bold mt-4">Empleados en el puesto</h3><ul class="list-group list-group-flush">@forelse($puesto->empleados as $empleado)<li class="list-group-item">{{ $empleado->nombre_completo }}</li>@empty<li class="list-group-item text-muted">Sin empleados.</li>@endforelse</ul></div>@endsection
